#include <iostream>
using namespace std;

void rocks(void) {
    
}

int main(void) {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while (t--)
        rocks();
    return 0;
}